// Generated by CoffeeScript 1.8.0
(function() {
  var Context, app, debug, http, normalizePort, onError, onListening, port, server;

  app = require('../main');

  debug = require('debug')('express:server');

  http = require('http');

  Context = require('../src/Context');

  normalizePort = function(val) {
    var port;
    port = parseInt(val, 10);
    if (isNaN(port)) {
      return val;
    }
    if (port >= 0) {
      return port;
    }
    return false;
  };

  onError = function(error) {
    var bind;
    if (error.syscall !== 'listen') {
      throw error;
    }
    bind = typeof port === 'string' ? 'Pipe ' + port : 'Port ' + port;
    switch (error.code) {
      case 'EACCES':
        Context.logger.log(bind + ' requires elevated privileges');
        return process.exit(1);
      case 'EADDRINUSE':
        Context.logger.log(bind + ' is already in use');
        return process.exit(1);
      default:
        throw error;
    }
  };

  onListening = function() {
    var addr, bind;
    addr = server.address();
    bind = typeof addr === 'string' ? 'pipe ' + addr : 'port ' + addr.port;
    return Context.logger.log('Listening on ' + bind);
  };

  port = normalizePort(process.env.PORT || '3000');

  app.set('port', port);

  server = http.createServer(app);

  server.listen(port);

  server.on('error', onError);

  server.on('listening', onListening);

}).call(this);

//# sourceMappingURL=www.js.map
